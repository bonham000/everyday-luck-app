import { Word } from "@src/content/types";

/** ========================================================================
 * Lesson 05
 * =========================================================================
 */

const Content: ReadonlyArray<Word> = [];

export default Content;
